//The ternary operator is a short way to write an if...else statement. It uses the symbols ? and :.

let age = 18;
let result = age >= 18 ? "Adult" : "Minor";
console.log(result);  //Adult

let number = 10;
let check = number % 2 === 0 ? "Even" : "Odd";
console.log(check); //Even